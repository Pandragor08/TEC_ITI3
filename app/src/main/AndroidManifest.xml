<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pandragor.tec_iti3">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TEC_ITI3">
        <activity android:name=".Verificar_telefono" />
        <activity android:name=".pdf_lector" />
        <activity android:name=".carreras" />
        <activity android:name=".Sesion" />
        <activity android:name=".Registrar" />
        <activity android:name=".visitante" />
        <activity android:name=".inicio_App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>